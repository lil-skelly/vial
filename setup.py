from setuptools import setup

if __name__ == '__main__':
    setup(
        name='vial',
        version='0.0.1',
        description='Vial is a lightweight python framework focused on exploiting Flask applications.',
        author='CopernicusPY',
        license="LICENSE",
        package_dir = {'': 'src'},
    )

